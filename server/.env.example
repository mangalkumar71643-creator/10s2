PORT=4000
JWT_SECRET=change-this-to-a-long-random-secret
# When true, OTPs are not sent anywhere - they're returned in the API response
# and printed to the server log, for local testing without an SMS provider.
OTP_TEST_MODE=true
